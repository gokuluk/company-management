package com.example.cockroachPoc.mapper;

import org.mapstruct.Mapper;

@Mapper(componentModel = "spring", uses = {})
public interface CompanyMapper {

//    Survey requestDtoToEntity(SurveyRequest questionnaireRequest);
}
